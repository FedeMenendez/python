Encapsula este código en una función llamada cuenta, y hazla genérica de tal modo que pueda aceptar una cadena y una letra como argumentos.

def cuenta (palabra,letra):
    contador = 0
    for letra in palabra:
        if letra == 'a':
            contador = contador + 1
    print(contador)
    
palabra = input ('ingrese una palabra: ')
letra = input ('ingrese una letra: ')
cuenta (palabra,letra)
